// Inicializa os dados da aplicação
const webApp = new Application();

// Inicia a aplicação quando os elementos da tela carregarem
document.addEventListener( 'DOMContentLoaded', () => webApp.onLoad() );
